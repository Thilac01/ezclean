name: Publish to PyPI
on:
  push:
    tags:
      - 'v*' # Trigger on version tags (e.g. v0.1.0)
  workflow_dispatch: # Allow manual triggering
jobs:
  build-n-publish:
    name: Build and publish Python distribution to PyPI
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Mandatory for trusted publishing via OIDC
      contents: read
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build
      - name: Build Package Distributions
        run: python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # No password/token parameter is needed here when using PyPI Trusted Publishing!
        # Set up a Trusted Publisher on pypi.org pointing to this repository and workflow name.
